module.exports = class Customer {
    constructor(id, name, locations, users) {
        this.id        = id
        this.name      = name
        this.locations = locations
        this.users     = users
    }
}
