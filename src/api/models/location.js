module.exports = class Location {
    constructor(id, name, customer, meters) {
        this.id       = id
        this.name     = name
        this.customer = customer
        this.meters   = meters
    }
}
